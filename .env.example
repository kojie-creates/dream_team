# Copy to .env (same directory) and fill in values. .env is git-ignored.

# Supabase (work queue — ticketing source of truth)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SECRET_KEY=your-service-role-key

{
  "name": "runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dream Team executable-core runtime (governed agent loop). Decoupled Node/TS module; no electron imports. See docs/ADR-001-executable-core.md.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "@supabase/supabase-js": "^2.106.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^2.1.9"
  }
}
